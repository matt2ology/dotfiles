##############################################################################
# Bash will try to source `.bash_profile` first, but if that doesn't exist, 
# it will source `.profile`
##############################################################################
source ~/.bashrc

# Recommended adding evaluation of this command's output to your dotfiles (e.g. `~/.profile`,
#  `~/.bash_profile`, or `~/.zprofile`) with: `eval "$(brew shellenv)"`
eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"
